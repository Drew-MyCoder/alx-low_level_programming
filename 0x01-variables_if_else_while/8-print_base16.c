#include <stdio.h>
/**
 * main - Prints numbers between 0 - 9and letters a - f
 *
 * Return: Always 0 (Success))
 */
int main(void)
{
	int k;
	for (k= 48; k < 58; k++)
	{
		putchar(i);
	}
	for (i = 97; i < 103; i++)
	{
		putchar('\n');
	return (0);
}
